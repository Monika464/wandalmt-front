import Cart from "../components/products/Cart";

const CartPage = () => {
  return (
    <div>
      {/* Dodaj layout, header, footer itp */}
      <Cart />
    </div>
  );
};

export default CartPage;
